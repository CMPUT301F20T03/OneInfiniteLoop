package com.example.booksies.model.books;

/**
 * This enum models the status of a book.
 * There are four status available: AVAILABLE, REUQESTED, ACCEPTED, BORROWED
 * @author: Michael Elgie (mbelgie)
 */
public enum book_status {
    AVAILABLE,
    REQUESTED,
    ACCEPTED,
    BORROWED
}
